#define logado 1
int login(char user[20]; char pass[30])
{
	char checkpass[20];
	char local[50]="Files/";
	strcat (local, "Users/");
	strcat (local, user);
	FILE *fp;
	fp = fopen ("Teste", "w");
	fscanf (fp, "Oi");
	fclose (fp);
}

int cadastro()
{
	FILE *fp;
	fp = fopen ("/Files/Users/Nome", "w");
	fprintf (fp, "Oi");
	fclose (fp);
}

int UserInfo()
{
}
